]
// action types

// TODO

// initial state

//const INITIAL_STATE = { ...db.badges };

// reducer

export default function reducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}

// actions

// TODO
