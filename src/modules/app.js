// action types

// TODO

// initial state

const INITIAL_STATE = {};

// reducer

export default function reducer(state = INITIAL_STATE, action) {
  switch (action.type) {
    default:
      return state;
  }
}

// actions

// TODO
